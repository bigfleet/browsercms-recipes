defaults: &defaults
  adapter: mysql
  host: <%= @host %>
  username: <%= @user %>
  password: <%= @passwd %>

production:
  database: <%= @database_stem %>_production
  <<: *defaults

staging:
  database: <%= @database_stem %>_staging
  <<: *defaults

development:
  database: <%= @database_stem %>_staging
  <<: *defaults